{
  "kind": "repi-worker-lease-scheduler-fixture",
  "schemaVersion": 1,
  "description": "WorkerLeaseSchedulerV1 fixture for lease_exclusive, heartbeat_required, stale_lease_recovery, work_stealing, duplicate_completion_rejected, claim_ref_preservation.",
  "negativeCases": [
    { "id": "negative:worker-lease-hash-drift", "mutate": "hashDrift", "expectedErrors": ["eventHash"] },
    { "id": "negative:worker-lease-missing-heartbeat", "mutate": "missingHeartbeat", "expectedErrors": ["heartbeatRequired"] },
    { "id": "negative:worker-lease-no-stale-recovery", "mutate": "noStaleRecovery", "expectedErrors": ["staleLeaseRecovered"] },
    { "id": "negative:worker-lease-duplicate-completion", "mutate": "duplicateCompletion", "expectedErrors": ["prevHash", "duplicateCompletion"] },
    { "id": "negative:worker-lease-missing-claim-refs", "mutate": "missingClaimRefs", "expectedErrors": ["claimRefsMissing"] },
    { "id": "negative:worker-lease-max-concurrency", "mutate": "maxConcurrencyViolation", "expectedErrors": ["maxConcurrencyRespected"] }
  ]
}
